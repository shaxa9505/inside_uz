extends layout

block content
  if errorCourses && errorCourses.length > 0
    p.alert.alert-danger #{errorCourses}
  .content-header: .container-fluid: .row.mb-2
    .col-sm-6.d-flex.justify-content-start: ol.breadcrumb.float-sm-right
      li.breadcrumb-item: a(href="/admin") Администратор
      li.breadcrumb-item: a(href="/admin/courses") Мои ученики
      li.breadcrumb-item.active #{title}
  .container
    .row.justify-content-center
      .col-lg-6
        form(method="POST" action=(courses ? `/admin/courseEdit/${courses._id}` : "#")).border.p-4.shadow-lg.rounded.wrapperForm
          .mb-3
            label.form-label.fs-4(for='course') Курс
            input#name.form-control.mb-2(type='text' name='course' value=(courses && courses.course ? courses.course : "") placeholder='Введите курс')
            label.form-label.fs-4(for='time') Время курса
            input#name.form-control.mb-2(type='number' name='time' value=(courses && courses.time ? courses.time : "") placeholder='Введите время вашего курса')
            label.form-label.fs-4(for='weekdays') Дни недели
            input#name.form-control.mb-2(type='text' name='weekdays' value=(courses && courses.weekdays ? courses.weekdays : "")  placeholder='Введите какие дни ваши уроки')
            label.form-label.fs-4(for='topic') Текущая тема
            input#name.form-control(type='text' name='topic' value=(courses && courses.topic ? courses.topic : "") placeholder='Введите какую тему преподаёте сейчас')
          button.btn.btn-dark.shadow-lg.w-100.text-light.mt-5(type='submit') Изменить